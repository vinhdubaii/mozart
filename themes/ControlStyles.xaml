<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        These styles have NO x:Key, so WPF applies them implicitly to every
        control of that TargetType in the app — this is what makes dark mode
        actually cover ComboBox/TextBox/CheckBox/RadioButton/Button everywhere,
        including inside SettingsWindow and BackgroundPickerDialog, without
        having to hand-apply a Style="..." to every single control usage.
        Everything references DynamicResource brushes from Themes/Light.xaml
        or Themes/Dark.xaml, so these re-color instantly when ThemeService
        swaps the active theme dictionary.
    -->

    <!-- ===================== TextBlock =====================
         Raw <TextBlock> elements without an explicit Foreground (there were
         several — e.g. in BackgroundPickerDialog) stayed default black text
         regardless of theme. This implicit style gives every TextBlock a
         theme-aware default; any explicit Foreground set locally still wins. -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    </Style>

    <!-- ===================== Button (default look, distinct from ChromeIconButton) ===================== -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ChromeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ChromeBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== TextBox ===================== -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ChromeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <!-- Symmetric padding. The old "6,4,6,6" (extra bottom padding to spare
             descenders from PART_ContentHost's clip) was the wrong fix for that
             problem: it left the content area vertically off-center, which at
             100% display scale was a rounding error small enough to not notice,
             but at 125%/150%+ WPF's per-monitor DPI pixel-snapping magnifies
             that same offset, plus the VerticalContentAlignment set on the
             TextBox in MainWindow/PrivateWindow/SettingsWindow was never wired
             into this template at all, so it did nothing. Real fix: symmetric
             padding, VerticalContentAlignment actually applied below, and the
             address-bar/search-box Border containers now use MinHeight instead
             of a hard Height so text is never asked to fit a box shorter than
             it needs at any scale. -->
        <Setter Property="Padding" Value="6,5,6,5"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <!-- VerticalAlignment="Center" (not Stretch) lets this size to
                             its actual text content and center that within the Border,
                             instead of being stretched to the Border's full height and
                             then relying on padding alone to center the text inside it -->
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ===================== CheckBox ===================== -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="box" Width="16" Height="16" CornerRadius="3"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource ChromeBorderBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Path x:Name="checkMark" Data="M2,7 L6,11 L14,2" Stroke="White" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== Extension Enable/Disable pill toggle =====================
         Deliberately x:Key'd (not implicit) so it never leaks onto the plain
         CheckBox look used everywhere else (e.g. "Show Bookmark Bar", "Ask
         where to save each file") — same reasoning as the checkbox-style
         look staying scoped in the rest of this file. Matches
         chrome://extensions' pill-shaped Enable/Disable switch instead of a
         checkmark box. -->
    <Style x:Key="ExtensionToggleSwitch" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="track" Width="36" Height="20" CornerRadius="10"
                            Background="{DynamicResource ChromeBorderBrush}">
                        <Border x:Name="knob" Width="16" Height="16" CornerRadius="8"
                                Background="White" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="knob" Property="Margin" Value="0,0,2,0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== RadioButton (default — NOT the sidebar nav one, which uses SettingsSidebarItem) ===================== -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="circle" Width="16" Height="16" CornerRadius="8"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource ChromeBorderBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Ellipse x:Name="dot" Width="8" Height="8" Fill="{DynamicResource AccentBrush}"
                                     Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="circle" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== ComboBox ===================== -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="itemBorder" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource SidebarSelectedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ChromeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="toggle" Focusable="False" ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource ChromeBorderBrush}" BorderThickness="1"
                                            CornerRadius="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="1" Text="⌄" Margin="0,0,10,0"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter x:Name="contentPresenter" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" HorizontalAlignment="Left"/>

                        <Popup x:Name="popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource ChromeBorderBrush}" BorderThickness="1"
                                    CornerRadius="6" MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="240" Margin="0,2,0,0">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- ===================== ContextMenu / MenuItem / Separator (hamburger menu ☰) =====================
         MenuButton_Click builds a plain ContextMenu/MenuItem/Separator entirely
         in code with no explicit styling — without these implicit styles the
         menu falls back to the OS default look, which reads as nearly
         transparent/low-contrast, especially in Dark mode. -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ChromeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.25" Color="Black"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Bumped 2026-08-22: FontSize 13 (was inherited default, ~12), Padding
         14,10 (was 10,6), icon slot 22x22 (was 16x16) - across the board
         bigger/easier to read/click, per request. The icon column width and
         Viewbox size inside each ContextMenuIcons.xaml template were bumped
         to match (see that file's own notes). -->
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="itemBorder" Background="Transparent" CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <!-- Named so the triggers below can collapse them to 0 -
                                     these are only meant to reserve space for an actual
                                     Icon / checkable state, not sit there empty on every
                                     plain text row (which is every hamburger-menu item
                                     except "Show Bookmark Bar"). Leaving them always-on
                                     wastes 48px on every row, including the widest one
                                     (the Zoom row), which is what was making the whole
                                     menu wider than Chrome/Edge's equivalent. -->
                                <ColumnDefinition x:Name="iconColumn" Width="28"/>
                                <ColumnDefinition x:Name="checkColumn" Width="20"/>
                                <!-- Auto, not *: MenuItem's ItemsHost (see the ContextMenu
                                     template above) is a plain vertical StackPanel. Grid
                                     falls back to content-sized behavior for "*" columns
                                     when measured with infinite available width anyway, so
                                     this alone doesn't change sizing - Auto is just the
                                     clearer, more literal way to say "size to the Header". -->
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                               Width="22" Height="22"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>

                            <Path x:Name="checkMark" Grid.Column="1" Data="M2,7 L6,11 L14,2"
                                  Stroke="{DynamicResource AccentBrush}" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  Width="16" Height="14" Visibility="Collapsed"/>

                            <ContentPresenter Grid.Column="2" ContentSource="Header"
                                               VerticalAlignment="Center" Margin="4,0,16,0"/>

                            <TextBlock Grid.Column="3" Text="{TemplateBinding InputGestureText}"
                                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource SidebarSelectedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                        <!-- Reclaim the icon column's 28px on every item that never sets
                             MenuItem.Icon (all of the hamburger menu, plus most of the
                             page context menu's items) instead of leaving it empty. -->
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="iconColumn" Property="Width" Value="0"/>
                        </Trigger>
                        <!-- Same for the checkmark column's 20px on every non-checkable
                             item (everything except "Show Bookmark Bar"). -->
                        <Trigger Property="IsCheckable" Value="False">
                            <Setter TargetName="checkColumn" Property="Width" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource ChromeBorderBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="6,5"/>
    </Style>

</ResourceDictionary>
