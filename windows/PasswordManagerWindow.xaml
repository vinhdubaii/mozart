<Window x:Class="MozartBrowser.Windows.PasswordManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Manager"
        Height="720" Width="720"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource ChromeBackgroundBrush}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="IconTextButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===================== Custom title bar ===================== -->
        <Grid Grid.Row="0" Height="40" Background="{DynamicResource ChromeBackgroundBrush}"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="14,0" VerticalAlignment="Center">
                <Path Style="{StaticResource SidebarIconPath}" Width="15" Height="15" Margin="0,0,8,0"
                      Data="M21.0667,5C21.6586,5.95805 22,7.08604 22,8.29344C22,11.7692 19.1708,14.5869 15.6807,14.5869C15.0439,14.5869 13.5939,14.4405 12.8885,13.8551L12.0067,14.7333C11.272,15.465 11.8598,15.465 12.1537,16.0505C12.1537,16.0505 12.8885,17.075 12.1537,18.0995C11.7128,18.6849 10.4783,19.5045 9.06754,18.0995L8.77362,18.3922C8.77362,18.3922 9.65538,19.4167 8.92058,20.4412C8.4797,21.0267 7.30403,21.6121 6.27531,20.5876C6.22633,20.6364 5.952,20.9096 5.2466,21.6121C4.54119,22.3146 3.67905,21.9048 3.33616,21.6121L2.45441,20.7339C1.63143,19.9143 2.1115,19.0264 2.45441,18.6849L10.0963,11.0743C10.0963,11.0743 9.3615,9.90338 9.3615,8.29344C9.3615,4.81767 12.1907,2 15.6807,2C16.4995,2 17.282,2.15509 18,2.43738 M17.8851,8.29353C17.8851,9.50601 16.8982,10.4889 15.6807,10.4889C14.4633,10.4889 13.4763,9.50601 13.4763,8.29353C13.4763,7.08105 14.4633,6.09814 15.6807,6.09814C16.8982,6.09814 17.8851,7.08105 17.8851,8.29353Z"/>
                <TextBlock Text="Password Manager" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="4,0">
                <Button Style="{StaticResource ChromeIconButton}" Click="MinimizeButton_Click" Content="—" ToolTip="Minimize"/>
                <Button x:Name="MaximizeButton" Style="{StaticResource ChromeIconButton}" Click="MaximizeButton_Click" Content="▢" ToolTip="Maximize"/>
                <Button Style="{StaticResource CloseButton}" Click="CloseButton_Click" Content="✕" ToolTip="Close"/>
            </StackPanel>
        </Grid>

        <!-- ===================== Search ===================== -->
        <Grid Grid.Row="1" Margin="20,16,20,8">
            <TextBox x:Name="SearchBox" Padding="8,6" TextChanged="SearchBox_TextChanged"/>
            <TextBlock Text="Search sites or usernames" IsHitTestVisible="False"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,0,0,0"
                       VerticalAlignment="Center" FontSize="12">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- ===================== Site list ===================== -->
        <Grid Grid.Row="2">
            <TextBlock x:Name="EmptyStateText" Text="No saved passwords yet."
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}"
                       Visibility="Collapsed"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,0,20,20">
                <ItemsControl x:Name="GroupsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander IsExpanded="False">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Domain}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding Accounts.Count, StringFormat=' ({0})'}" FontSize="12"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Expander.Header>

                                <ItemsControl ItemsSource="{Binding Accounts}" Margin="16,8,0,4">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="0,8" BorderThickness="0,0,0,1"
                                                    BorderBrush="{DynamicResource SurfaceAltBrush}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Username}" FontSize="12"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                        <TextBlock Text="{Binding RevealedPassword}" FontSize="12" Margin="0,2,0,0"
                                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                    </StackPanel>

                                                    <Button Grid.Column="1" Content="{Binding RevealButtonLabel}"
                                                            Style="{StaticResource IconTextButton}" Margin="8,0"
                                                            Click="RevealButton_Click" Tag="{Binding}"/>

                                                    <Button Grid.Column="2" Content="Delete"
                                                            Style="{StaticResource IconTextButton}"
                                                            Click="DeleteButton_Click" Tag="{Binding}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
